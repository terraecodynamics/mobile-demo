/**
 * Farm boundary demo modes
 *
 * COLLECTION (true)  — Draw / Modify / Save JSON / radius chips (data capture)
 * PLAYBACK (false)   — user drags analysis radius → Confirm → show saved
 *                      boundaries from JSON for that radius (chips hidden)
 *
 * Collection is done — keep false for normal demo use.
 */
export const FARM_BOUNDARY_COLLECTION_MODE = false;
